#include <stdio.h>

/**
 * main - prints numbers from 0 to 9.
 *
 * Return: Always ) (success)
 */
int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
	{
		printf("%d", i);
	}
	putchar('\n');
	return (0);
}
